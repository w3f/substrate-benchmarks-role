run_module_benchmarks: false
capture_module_db_logs: false
run_node_benchmarks: true
run_filesystem_io_benchmarks: true

# Set `scope` to `limited` in order to only run
# `balances` and `staking` module benchmarks.
#
# scope: limited